{% extends 'base.html.twig' %}

{% block title %}Confirmation de Lecture{% endblock %}

{% block body %}
    <!-- Utilisation de classes Tailwind CSS. Assurez-vous que Tailwind est inclus via 'base.html.twig' ou CDN. -->
    <div class="min-h-screen flex items-center justify-center p-4 sm:p-6 bg-gray-50">

        <!-- Conteneur principal - Carte de confirmation -->
        <div class="max-w-xl w-full bg-white shadow-2xl rounded-xl p-8 sm:p-12 text-center border-t-8 border-indigo-500 transition duration-300">

            <!-- Icône de Succès -->
            <div class="mx-auto h-16 w-16 flex items-center justify-center rounded-full bg-indigo-100 mb-6">
                <!-- SVG de coche -->
                <svg class="h-10 w-10 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
            </div>
            
            <!-- Titre Principal -->
            <h1 class="text-3xl sm:text-4xl font-extrabold text-gray-900 mb-4">
                Lecture Confirmée !
            </h1>

            <!-- Message de Confirmation Détaillé avec variables Twig -->
            <p class="text-lg text-gray-700 leading-relaxed mb-8">
                Bonjour <strong class="text-indigo-600">{{ partner.name }}</strong>,
                <br class="hidden sm:inline">
                Votre confirmation de lecture pour le document :
            </p>
            
            <!-- Document mis en évidence -->
            <div class="bg-indigo-50 border border-indigo-200 text-indigo-800 p-4 sm:p-5 rounded-lg mb-8 inline-block shadow-inner">
                <span class="font-semibold text-xl block truncate max-w-xs sm:max-w-none">
                    "{{ document.title }}"
                </span>
            </div>

            <!-- Message de clôture -->
            <p class="text-md text-gray-600 mb-8">
                Nous vous remercions d'avoir pris le temps de consulter ce document essentiel. Bonne journée et bon travail !
            </p>

            <!-- Bouton de Retour, ajustez la route si nécessaire -->
            {# Remplacez 'admin_document_index' par la route réelle vers votre tableau de bord #}
            <a href="{{ path('admin_document_index') }}" class="inline-flex items-center justify-center px-6 py-3 border border-transparent text-base font-medium rounded-full shadow-lg text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-150 ease-in-out transform hover:scale-[1.02]">
                Retourner au Tableau de Bord
            </a>

        </div>

    </div>
{% endblock %}